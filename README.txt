Import discord.py and discord slash commands via pip:
pip install discord.py
pip install discord-py-slash-command


add your own guild ids into the bot and discord bot token

run the bot via cmd by doing:
cd [file path]
python chat-logger-main.py